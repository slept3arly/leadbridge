-- LeadActivity table was already dropped by the previous (partially successful) migration attempt
-- AuditLog table was already dropped by the previous (partially successful) migration attempt
-- ActivityType enum was already dropped by the previous (partially successful) migration attempt

-- No further changes needed: ActionType, ResponseType, InterestType are still used by ActivityEntry
